// Empty stub mapped via vitest alias for the `server-only` package, which
// is a Next.js runtime marker not available outside the Next bundler. The
// guard is enforced at build time; tests run under jsdom and don't need
// the real module to exist.
export {}
